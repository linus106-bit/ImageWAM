defaults:
  - libero_omnigen2_pair
  - _self_

train:
  num_frames: null
  observation_chunk_count: ${model.chunkwise_causal.num_chunks}
  actions_per_chunk: ${model.chunkwise_causal.actions_per_chunk}

